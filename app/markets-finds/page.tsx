import { Footer, Header, PageHero } from "../components"; import { finds } from "../content";
export default function Page(){return <main><Header/><PageHero kicker="Good things, found slowly" title="Markets & Finds" intro="Objects with a story, practical souvenirs, and a few excellent things no suitcase needed."/><section className="shell archive-grid finds-archive">{finds.concat(finds.slice(0,2)).map((f,i)=><article className="find-archive-card" key={i}><div className="photo" style={{backgroundImage:`url('${f.image}')`}}></div><p className="catalog">CATALOG № {String(i+1).padStart(2,"0")} · {f.place}</p><h2>{f.name}</h2><p>{f.note}</p></article>)}</section><Footer/></main>}
