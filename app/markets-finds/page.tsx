import type { Metadata } from "next"; import { Footer, Header, PageHero } from "../components"; import { finds } from "../content"; import { FindObject } from "./find-object";
export const metadata:Metadata={title:"Markets & Finds",description:"Objects with a story, practical souvenirs, and excellent things gathered from markets and small shops."};
export default function Page(){return <main className="finds-page"><Header/><PageHero kicker="Good things, found slowly" title="Markets & Finds" intro="Objects with a story, practical souvenirs, and a few excellent things no suitcase needed."/><aside className="finds-ledger shell" aria-label="Collection notes"><span><b>{String(finds.length).padStart(2,"0")}</b> objects catalogued</span><span>Markets, shops &amp; accidental discoveries</span><i>Each one made it home</i></aside><section className="shell finds-cabinet">{finds.map((find,index)=><FindObject find={find} index={index} key={find.name}/>)}</section><Footer/></main>}
